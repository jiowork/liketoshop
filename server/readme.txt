# npm init        //安装依赖
# node server.js   // 运行服务




使用koa框架，自建————npm init

使用插件中间件：
    koa-router——路由，
    koa-bodyparser——post提交数据中间件，
    koa-static——static静态文件
    koa-multer——上传文件中间件
    koa2-cors——解决跨域
    jwt-simple——生成解析token
